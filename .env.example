# EVENTS_SOURCE=mock|live (defaults to "live" when unset)
# "mock" forces the hardcoded fixtures in src/data/events.ts, useful for
# local dev without hitting the live GDELT API, or as an instant rollback
# in production if GDELT ever misbehaves. No API key is needed for GDELT.
EVENTS_SOURCE=live
